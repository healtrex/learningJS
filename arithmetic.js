const add = a => b => a + b;

const subtract = a => b => a - b;

const multiply = a => b => a * b;

const divide = a => b => a / b;

const square = a => a * a;

const power = a => b => a ** b;

console.log(power(10)(2)); //10^2=100
